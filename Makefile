dev-web:
	@echo "Task 1 scaffold only: web app code is added in later tasks."

dev-agent:
	@echo "Task 1 scaffold only: agent service code is added in later tasks."

test-web:
	@echo "Task 1 scaffold only: web tests are added in later tasks."

test-agent:
	@echo "Task 1 scaffold only: agent tests are added in later tasks."
